import { createContext } from 'react';

import AbstractSidebarDefaultRouterLink from './AbstractSidebarDefaultRouterLink';

const AbstractSidebarRouterLinkContext = createContext(AbstractSidebarDefaultRouterLink);

export default AbstractSidebarRouterLinkContext;
